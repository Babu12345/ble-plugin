[package]
name = "plugin_config"
version = "0.1.0"
edition = "2021"

[dependencies]
heapless = "0.9.1"
protocol = { path = "../protocol", default-features = false }

[features]
default = ["std", "protocol_buffer"]
std = ["protocol/std"]
protocol_buffer = ["protocol/protocol_buffer"]
