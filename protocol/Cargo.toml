[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
# Custom
lib_utils = { path = "../lib_utils" }

# Crates
heapless = { version = "0.8.0" }
rmp-serde = "1.3.0"
serde = { version = "1.0.219", features = ["derive"] }
serde_bytes = "0.11.17"
